// DOC: the home grid is rendered by Shell (it stays mounted behind every route), so the
//  index route itself contributes nothing to the middle section.
const Home = () => null;

export default Home;
